# AGENTS.md

## Creation H5 响应式约束

- Creation 顶部积分胶囊仅展示积分图标和余额，不再展示右侧青色加号按钮；购买或充值入口不得通过该胶囊内嵌加号恢复。
- `<768px` 时不得直接纵向铺开桌面侧边栏；页面使用 56px 吸顶顶栏，导航通过左侧抽屉打开，桌面端继续保留 248px/84px 的展开与收起形态。
- H5 顶栏必须保留导航入口、品牌标识、积分与账户入口；Home 内容区不得重复展示账户区，避免挤占首屏。
- H5 导航抽屉必须支持遮罩点击、关闭按钮和 `Esc` 关闭，打开时锁定背景滚动并限制键盘焦点在抽屉内，关闭后焦点返回导航触发按钮；切换一级页面后自动关闭。
- H5 首屏优先展示 Agent 标题、带模板参考图的 Prompt 输入框和二级任务标题横向入口；不得展示一级任务卡片或快捷参数填选区，模板推荐区不得用大块空白标题区域挤压卡片。
- 触屏端核心 CTA 不得仅依赖 hover 显示；Home 模板预览卡片不得渲染 `Use same style` 覆盖按钮，整张卡片继续作为模板入口，避免图片上叠加操作层。
- 调整 Creation H5 布局时，至少以窄屏和桌面两档验证：页面无横向溢出、抽屉不遮挡触发按钮、账户入口可用、导航关闭与页面切换正常、首屏能看到 `What's new` 或其上沿。
- H5 Home 内容采用紧凑密度：Agent 标题不超过 23px，Prompt 卡片使用较小内边距与最小高度；`What's new` 卡片约 120px 高、推荐工具入口约 72px 高、模板预览卡片约 180px 高，桌面断点以上恢复原尺寸。
- H5 区块标题统一使用紧凑字号；调整密度时不得造成核心 CTA、二级任务标题横向入口或模板横向滑动入口不可见。
- H5 Recommended tools 必须按现有顺序仅展示前 7 个高频工具和 `More`，组成两行四列轻量网格；图标在上、标题在下，不得展示卡片背景、边框和描述，描述仅可保留在无障碍文本中。其余工具仅在 Tools 页面展示，桌面端继续保留完整推荐大卡片和工具网格。
- Home 模板预览卡片在 H5 与桌面端均不得展示 `Use same style` 按钮；模板名称继续展示，整张卡片保持可点击和键盘可访问。
- Agent Send 使用仅图标的圆形按钮，与 Add media、Auto、Assets、Prompt/knowledge 和任务胶囊保持同一底部工具栏；按钮必须保留 `aria-label="Send"` 和 title，不得单独换行或恢复文字按钮。
- H5 点击头像必须打开全屏底部上滑账户面板，面板从底部进入并覆盖页面；顶部展示账号头像、名称和邮箱，中部固定展示 Credits、Plan、Setting、Language，Log out 使用底部固定操作位。支持右上角关闭、遮罩点击、Esc 和焦点返回头像按钮。

## Creation Home 模板推荐内容约束

- Home 底部模板推荐的顶部一级分类顺序固定为 `Popular → Creative Effects → Beauty → E-Commerce → Lifestyle → Seasonal`。
- 一级分类与左侧对应分类必须使用数据驱动的一对多结构；点击顶部一级分类时定位到该分类的第一组内容块，同组内容块连续排列。
- 左侧分类名称与说明文案以 `prd/creation-page-design-functional-spec.md` 的“Home 模板推荐”表格为唯一口径，需求中空白的说明保持不展示，不得自行补充泛化文案。
- 调整 Home 模板推荐分类或文案时，必须同步更新数据测试和 `prd/creation-page-design-functional-spec.md`。
- 右侧模板预览继续复用 `public/assets/creation` 下的本地资源；未提供新的模板标题或素材时不得擅自替换现有预览内容。

## Creation Create New 工具菜单约束

- 点击侧边栏 `Create New` 必须打开浅色非模态工具菜单；桌面端在按钮右侧展开，窄屏在按钮下方展开，不得遮挡侧边栏按钮本身。
- 菜单标题固定为 `All Tools`，右上角 `View all` 必须关闭菜单并进入完整 Tools 页面。
- 菜单与 Tools 页面必须共用 `src/lib/creation-tools.ts` 作为工具名称、分组、图标和路由的唯一数据源，禁止维护重复清单。
- 分组固定为 `Image Tools`、`Video Tools`、`Creative Utilities`；每个分组使用独立浅灰卡片并逐项展示工具图标和名称。
- 已配置真实路由的工具必须直接跳转；暂未配置路由的工具统一关闭菜单并进入 Tools 页面，不得构造不存在的页面地址。
- 菜单必须支持点击外部关闭、`Esc` 关闭并将焦点返回触发按钮；打开后焦点进入首个工具，触发按钮同步暴露展开状态。
- 调整 Create New 菜单或 Tools 目录时，必须同步更新 `prd/creation-page-design-functional-spec.md` 和工具目录数据测试。

## Creation 页面快速任务约束

- 桌面端 Agent 下方快速任务保持两级结构：一级负责分类导航，二级负责具体任务选择。
- `<768px` 的 H5 不展示一级分类卡片，必须将全部 16 个二级任务名称直接展示为单行横向滚动的紧凑标题按钮；按钮只展示标题，使用选中态标识当前任务，不展示描述、图标或预览图。
- H5 首次进入时自动选择全部二级任务中的第一项，并立即将该任务的模板参考图和真实 Prompt 回填到输入框；切换标题按钮时同步替换模板参考图和 Prompt，同时保留用户上传图片。
- 一级卡片只展示图标和标题，不得增加副标题、描述或预览图片。
- 二级卡片必须采用模板预览样式，可见内容仅包含顶部简短描述和下方本地预览图片，不得在卡片内重复展示任务标题或图标；任务名称保留在选中后的任务胶囊和无障碍标签中。四张卡片保持单行横向排列，可用宽度不足时使用横向滚动。
- 二级卡片描述必须直接贴合对应任务标题，使用具体动作说明模板用途，不得使用可套用于其他任务的泛化文案；描述最多两行。
- 二级卡片与图片容器必须使用固定高度，图片以 `object-cover` 裁切；描述最多展示两行，避免加载、换行或悬停时改变卡片布局。
- 当前一级分类固定为 `E-commerce Poster`、`Amazon Detail Images`、`E-commerce Video` 和 `Trending AI Videos`，每个分类包含四个二级任务。
- 点击一级分类后必须切换二级任务列表并自动选中第一个任务；二级列表上方不得展示分类标题或 `Back`。
- 桌面端退出二级任务列表的唯一入口为输入区任务胶囊的关闭按钮；关闭后必须清空任务及参数并返回一级列表。H5 始终展示全部二级标题，不提供返回一级列表的流程。
- 每个二级任务必须配置可直接使用的 Prompt、文本参数默认值和下拉参数默认值；点击任务后必须将这些真实模板数据同步回填到输入区，不能只切换占位文案。
- Prompt 必须使用可编辑的真实输入控件；切换二级任务时覆盖为新模板 Prompt 和参数默认值，再次点击当前任务时也必须恢复该模板默认数据。
- 桌面端任务胶囊必须绑定当前选中的二级任务图标、颜色和名称；点击或切换二级任务时胶囊内容必须同步更新。关闭任务胶囊后返回一级列表并清空任务参数及模板示例图片，但保留当前可编辑 Prompt 和用户上传图片。H5 不展示任务胶囊，当前任务由下方标题按钮的选中态表达。
- 桌面端任务胶囊必须位于输入框底部左侧工具栏，并紧跟在 Prompt/knowledge 按钮之后；快捷参数独立保留在输入框顶部，不得与任务胶囊同行。
- H5 输入框顶部必须保留模板参考图并隐藏文本参数、下拉参数等快捷填选区域；Prompt 仍使用可编辑真实输入控件，底部通用媒体与生成工具栏保持可用。
- 快捷参数保持紧凑：文本参数直接显示模板默认值，清空后展示完整输入提示；下拉框关闭时只显示当前值，不得拼接或叠加参数名。
- 参数名称必须作为不可选择的分组标题显示在下拉菜单弹层顶部，选项位于标题下方；不得在关闭状态控件内部或控件外增加参数名称标签。
- 文本参数与下拉框保持相同高度；参数较多时整行按控件底部对齐，控件不得遮挡相邻参数或 Prompt。
- 所有二级任务共用输入区底部的 `Add media` 图片入口，不得增加模板专属上传框；当前 16 个二级任务均需要至少一张图片，选中任务时必须将该任务配置的一张或多张本地示例图作为模板素材回填到输入区最上方独立一行。
- 模板图片数量必须按任务语义配置：`Comparison & Trust A+ Set` 使用“主商品图 + 对比商品图”，`Before & After` 使用“Before + After”，`Kiss Cam` 和 `The Final Hug` 均使用“人物一 + 人物二”；其余任务保持单图。
- 模板示例图片与用户上传图片必须区分来源：切换任务时整组替换模板示例图并保留用户图片；再次点击当前任务时恢复完整模板图片组；关闭任务时移除全部模板示例图并保留用户图片。
- 只有用户上传图片使用对象 URL 并计入最多 10 张的限制；删除模板示例图时不得调用 `URL.revokeObjectURL`，避免错误处理静态资源。
- 快速任务预览图优先复用 `public/assets/creation` 下的本地资源，避免引入不稳定的远程图片依赖。
- 调整快速任务时必须同步更新 `prd/creation-page-design-functional-spec.md` 中的分类、卡片规则和快捷参数说明。

## Creation 侧边栏促销卡片约束

- 侧边栏底部的 `Price` 入口必须以独立促销卡片展示，并固定放在 `Blog` 上方；底部资源顺序为 `Price → Blog → Language`，不得退化为普通资源链接。
- 展开状态采用约 56px 的简洁固定高度，按“优惠图标、`Price 50% OFF`、最右侧右箭头”单行排列；整张卡片作为套餐入口，不得增加 CTA 文案、会员层级、装饰光斑、冗余利益点或独立胶囊按钮。使用高对比渐变、白色文字和可见焦点态，保证可读性与键盘访问。
- 桌面端收起侧边栏时，促销卡片必须压缩为居中的优惠图标按钮并保留 tooltip 与无障碍名称；窄屏侧边栏保持完整卡片内容。
- 促销卡片不得遮挡底部资源入口，也不得改变右上角积分与账户区域。

## Creation AI Voice 卡片约束

- Templates 的 AI Voice 卡片只承载播放/暂停和视频交接操作，不得设置默认选中项、整卡选中交互、选中背景、选中描边或勾选标记。
- 播放按钮必须保持单实例播放：切换声音前停止当前试听，再次点击当前声音时暂停；播放结束、失败、离开 AI Voice 或组件卸载时恢复默认状态。
- 视频 CTA 统一使用 `Generate Video`，与 Creation 内其他视频生成入口保持一致，并携带声音名称、语言、口音和性别进入 `/ugc-video-generator`。
- 声音跳转参数和服务端 Prompt 参数必须通过同一白名单校验并恢复为规范数据；缺失、篡改、未知或超长参数不得进入视频生成链路。
- E-commerce Video 接收到有效声音后，必须在创作表单中可见展示声音名称、语言、口音和性别，并将其传入视频 Prompt 优化接口。
- 调整 AI Voice 卡片或交接协议时必须同步更新 `prd/creation-page-design-functional-spec.md`，并覆盖声音参数单元测试和浏览器跳转验证。

## AI Video 参考模式约束

- AI Video 页面不设置独立的“文生视频”按钮，只展示“全能参考”和“首尾帧”两个模式；全能参考素材区为空时使用原有文生视频模型并调用 `text_to_video`，存在任意合法图片、视频或音频时调用 `reference_image_to_video`。
- 原参考图模式由 `image_to_video` 升级为全能参考，统一调用 `reference_image_to_video`；全能参考支持的图片、视频、音频类型及上传限制全部以当前模型接口返回结果为准。
- 全能参考无论当前只上传图片还是混合上传多媒体，均调用 `reference_image_to_video`；不同素材必须保留真实媒体类型和上传顺序，不得将视频或音频放入图片字段。
- 首尾帧模式的首帧必填、尾帧选填：仅首帧时调用 `image_to_video`，首帧和尾帧齐全时调用 `first_last_frame_to_video`；仅尾帧不得生成，删除首帧时不得把尾帧自动提升为首帧。
- 服务端必须根据模型能力、素材数量和首尾帧角色复核 `aigc_category`，不得信任前端直接传值，也不得在素材缺失或专用接口失败时静默降级。
- 上传限制必须按照 `prd/ai-video-upload-limits-spec.md` 展示具体原因；限制文案优先使用“当前模型支持/仅支持……”说明模型边界，再展示文件实际值，操作异常直接说明失败对象和下一步。已知限制不得统一降级为模糊的“上传失败”，也不得直接展示上游原始响应。
- AI Video 页面模式入口顺序固定为“全能参考 → 首尾帧”；上传素材在当前创作会话内跨模型和模式复用，兼容素材继续展示和使用，不兼容素材保留原素材位及上传顺序后暂存隐藏且不参与请求。数量超限时按原上传顺序保留允许数量内的素材。未在同一素材位上传新素材时，切回兼容模型或模式必须恢复旧素材；同一素材位的新上传替换旧素材，用户主动删除则永久移除。
- 全能参考切换到首尾帧时，按上传顺序将前两张兼容图片映射为首帧和尾帧；首尾帧切换到全能参考时，按首帧、尾帧顺序复用为参考图片。切换产生的隐藏素材不阻塞生成，当前模型下新上传且校验失败的素材仍需阻塞并提示具体原因。
- AI Video 底部下发输入区的视觉层级固定为“参考素材带 → Prompt 主编辑区 → 参数与生成工具栏”；窄屏参数横向滚动且生成按钮整行可用，桌面端参数与生成操作保持单行，触屏端素材删除操作不得仅依赖 hover。
- 调整 AI Video 输入模式或接口分流时，必须同步更新 `prd/ai-video-reference-mode-upgrade-prd.md`；调整上传能力、校验或异常文案时，还必须同步更新 `prd/ai-video-upload-limits-spec.md`。文档优先使用单一分流表表达完整规则，避免重复铺陈，并验证四种 category 分流、无首帧阻断及具体限制原因展示。

## 验证要求

- 代码变更后至少执行 `npx tsc --noEmit`。
- 在 `/creation` 验证 Create New 菜单的桌面右侧定位、窄屏下方定位、三组工具完整性、`View all`、真实工具路由、无路由工具回退、点击外部关闭与 `Esc` 焦点恢复。
- 在 `/creation` 验证桌面端一级四列、二级单行四卡；窄屏不得展示一级卡片，全部 16 个二级任务标题必须保持单行并可横向滚动。
- 桌面端至少验证一个一级分类进入后的首项自动选中，以及通过任务胶囊关闭按钮返回一级列表的流程。
- 窄屏首次进入必须自动选中第一个二级任务，输入框可见模板参考图与真实 Prompt、不可见快捷参数填选区和任务胶囊；连续切换两个标题按钮时，选中态、参考图和 Prompt 必须同步更新。
- 窄屏 Recommended tools 必须验证仅有 7 个高频工具和 `More`，正好两行且每行四个入口；图标位于标题上方，无描述与卡片底色，并确保页面无横向溢出。其余工具仍需在 Tools 页面可见，桌面端完整重点大卡与右侧工具网格不得被 H5 样式覆盖。
- 验证任务胶囊位于底部左侧 Prompt/knowledge 按钮之后，且不再占用顶部快捷参数行。
- 进入任一一级分类后，任务胶囊必须展示自动选中的首个二级任务名称；连续切换至少两个二级任务，胶囊图标、颜色和名称必须与当前二级任务同步更新。
- 至少验证两个二级任务的快捷参数默认值、下拉框关闭状态的纯值展示，以及展开菜单顶部的不可选参数名称标题，并验证 `Add media` 选择图片后缩略图位于参数行上方独立一行，以及缩略图删除和任务切换后的保留状态。
- 至少验证两个二级任务被点击后，Prompt、文本参数和下拉参数均回填为各自模板数据；编辑 Prompt 后切换任务应覆盖为新模板 Prompt，再次点击当前任务应恢复其模板默认值。
- 至少验证一个单图任务和一个双图任务：`Kiss Cam` 被选中后必须出现两张来源不同、用途明确的模板图；切换任务时整组示例图被替换，关闭任务时全部模板图被移除，且用户上传图片不受影响。
- 检查二级卡片不显示任务标题或图标，简短描述位于图片上方且最多两行；验证图片加载、固定高度和裁切效果，确保四张卡片等高且内容无重叠。
- 逐项检查 16 个二级卡片描述与任务标题的语义匹配度，确保描述能明确区分对应模板用途。
- 验证侧边栏展开时底部资源顺序为 `Price → Blog → Language`，优惠图标、`Price 50% OFF` 和最右侧右箭头单行完整可见；桌面端收起后仅显示优惠图标且无布局溢出。
- 验证 AI Voice 卡片不存在默认或点击选中态，播放/暂停状态互斥，`Generate Video` 携带规范声音参数进入 E-commerce Video，且目标页可见展示并透传该声音配置。
